{
  "name": "lista-servidor",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Servidor de sincronizacao do app Lista & Estoque",
  "engines": { "node": ">=22" },
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test --test-concurrency=1 test/*.test.js"
  },
  "dependencies": {
    "express": "^5.2.1",
    "pg": "^8.23.0"
  }
}
